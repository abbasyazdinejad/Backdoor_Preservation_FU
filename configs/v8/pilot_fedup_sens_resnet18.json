{
  "exp_id": "pilot_resnet18_lr0p05",
  "dataset": "cifar10",
  "rounds": 30,
  "partition": {
    "scheme": "iid",
    "beta": null
  },
  "notes": "ResNet-18 (CIFAR variant) learning-rate/round pilot, lr=0.05. PILOT (v7 hyperparameter selection, validation split only): 5000 training images are held out (seed-determined) before partitioning and used for every evaluation; the test set is never evaluated. Seed 100 is not a reported seed. Selection rules were fixed in audit/v7_hyperparameter_selection_protocol.md before this pilot was run. | v8: FedUP pruning-percentage sensitivity on the validation split (P in {5,10,20}%); P = 10% is taken from the source and is not selected here.",
  "data_root": "data",
  "architecture": "resnet18",
  "num_clients": 20,
  "malicious_clients": [
    0,
    1,
    2,
    3
  ],
  "poison_rate": 0.5,
  "target_label": 0,
  "trigger": {
    "size": 4,
    "value": 1.0,
    "offset": 1
  },
  "local_epochs": 2,
  "batch_size": 64,
  "optimizer": "sgd",
  "lr": 0.05,
  "momentum": 0.9,
  "weight_decay": 0.0,
  "clients_per_round": 1.0,
  "history_every": 2,
  "seeds": [
    100
  ],
  "alphas": [
    1.0
  ],
  "methods": {
    "short_retrain": {
      "rounds": 5
    },
    "fedup": {
      "prune_fraction": 0.1,
      "recovery_rounds": 5
    }
  },
  "device": "auto",
  "results_dir": "results/raw/pilots_fedup_sensitivity",
  "checkpoints_dir": "checkpoints_v8/pilot",
  "logs_dir": "logs_v8/pilot",
  "validation_holdout": 5000,
  "eval_split": "validation",
  "eval_every": 2,
  "compromised_results_dir": "results/raw/pilots_hyperparameter_selection",
  "compromised_checkpoints_dir": "checkpoints_v7/pilot"
}